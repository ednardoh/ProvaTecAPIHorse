program CadEndereco;

uses
  System.StartUpCopy,
  FMX.Forms,
  uCadEndereco in 'uCadEndereco.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
